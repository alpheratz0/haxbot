import { AdministrativeCommand, GameCommandContext } from '..';
import { LanguageProvider } from '../../../langs/language-provider';
import { colors } from '../../../room/configuration';
import { Futsalx3 } from '../../../stadiums/futsal-x3';

export const x3Command = new AdministrativeCommand(
	'x3',
	({ sender, room }: GameCommandContext) => {
		if (room.getScores())
			return room.sendAnnouncement(
				LanguageProvider.get('Game cannot be started.'),
				sender.id,
				colors.error
			);

		room.setCustomStadium(Futsalx3);
	}
);
	LanguageProvider.get('Game cannot be started.'),
				sender.id,
				colors.error
			);

		room.setCustomStadium(Futsalx3);
	}
);
	LanguageProvider.get('Game cannot be started.'),
				sender.id,
				colors.error
			);

		room.setCustomStadium(Futsalx3);
	}
);
	LanguageProvider.get('Game cannot be started.'),
				sender.id,
				colors.error
			);

		room.setCustomStadium(Futsalx3);
	}
);
	LanguageProvider.get('Game cannot be started.'),
				sender.id,
				colors.error
			);

		room.setCustomStadium(Futsalx3);
	}
);
	LanguageProvider.get('Game cannot be started.'),
				sender.id,
				colors.error
			);

		room.setCustomStadium(Futsalx3);
	}
);
	LanguageProvider.get('Game cannot be started.'),
				sender.id,
				colors.error
			);

		room.setCustomStadium(Futsalx3);
	}
);
	LanguageProvider.get('Game cannot be started.'),
				sender.id,
				colors.error
			);

		room.setCustomStadium(Futsalx3);
	}
);
	LanguageProvider.get('Game cannot be started.'),
				sender.id,
				colors.error
			);

		room.setCustomStadium(Futsalx3);
	}
);
	LanguageProvider.get('Game cannot be started.'),
				sender.id,
				colors.error
			);

		room.setCustomStadium(Futsalx3);
	}
);
	LanguageProvider.get('Game cannot be started.'),
				sender.id,
				colors.error
			);

		room.setCustomStadium(Futsalx3);
	}
);
	LanguageProvider.get('Game cannot be started.'),
				sender.id,
				colors.error
			);

		room.setCustomStadium(Futsalx3);
	}
);
	LanguageProvider.get('Game cannot be started.'),
				sender.id,
				colors.error
			);

		room.setCustomStadium(Futsalx3);
	}
);
	LanguageProvider.get('Game cannot be started.'),
				sender.id,
				colors.error
			);

		room.setCustomStadium(Futsalx3);
	}
);
	LanguageProvider.get('Game cannot be started.'),
				sender.id,
				colors.error
			);

		room.setCustomStadium(Futsalx3);
	}
);
	LanguageProvider.get('Game cannot be started.'),
				sender.id,
				colors.error
			);

		room.setCustomStadium(Futsalx3);
	}
);
	LanguageProvider.get('Game cannot be started.'),
				sender.id,
				colors.error
			);

		room.setCustomStadium(Futsalx3);
	}
);
	LanguageProvider.get('Game cannot be started.'),
				sender.id,
				colors.error
			);

		room.setCustomStadium(Futsalx3);
	}
);
	LanguageProvider.get('Game cannot be started.'),
				sender.id,
				colors.error
			);

		room.setCustomStadium(Futsalx3);
	}
);
	LanguageProvider.get('Game cannot be started.'),
				sender.id,
				colors.error
			);

		room.setCustomStadium(Futsalx3);
	}
);
	LanguageProvider.get('Game cannot be started.'),
				sender.id,
				colors.error
			);

		room.setCustomStadium(Futsalx3);
	}
);
	LanguageProvider.get('Game cannot be started.'),
				sender.id,
				colors.error
			);

		room.setCustomStadium(Futsalx3);
	}
);
	LanguageProvider.get('Game cannot be started.'),
				sender.id,
				colors.error
			);

		room.setCustomStadium(Futsalx3);
	}
);
	LanguageProvider.get('Game cannot be started.'),
				sender.id,
				colors.error
			);

		room.setCustomStadium(Futsalx3);
	}
);
	LanguageProvider.get('Game cannot be started.'),
				sender.id,
				colors.error
			);

		room.setCustomStadium(Futsalx3);
	}
);
	LanguageProvider.get('Game cannot be started.'),
				sender.id,
				colors.error
			);

		room.setCustomStadium(Futsalx3);
	}
);
	LanguageProvider.get('Game cannot be started.'),
				sender.id,
				colors.error
			);

		room.setCustomStadium(Futsalx3);
	}
);
	LanguageProvider.get('Game cannot be started.'),
				sender.id,
				colors.error
			);

		room.setCustomStadium(Futsalx3);
	}
);
	LanguageProvider.get('Game cannot be started.'),
				sender.id,
				colors.error
			);

		room.setCustomStadium(Futsalx3);
	}
);
